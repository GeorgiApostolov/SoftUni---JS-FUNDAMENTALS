function solve(num) {
    let output = ``;
    for (let i = 0; i < num; i++) {
        output += `${num} `;

    }
    for (let j = 0; j < num; j++) {
        console.log(output);
    }

}
solve(3);
solve(7);
solve(2);